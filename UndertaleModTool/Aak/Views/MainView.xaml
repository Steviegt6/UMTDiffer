<UserControl x:Class="UndertaleModTool.Aak.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UndertaleModTool.Aak.Views"
             xmlns:solution="clr-namespace:UndertaleModTool.ViewModels.Solution"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <solution:MainViewModel />
    </UserControl.DataContext>
    <TreeView ItemsSource="{Binding Solution.Projects}">
        <TreeView.Resources>
            <HierarchicalDataTemplate DataType="{x:Type solution:ProjectViewModel}" ItemsSource="{Binding Items}">
                <TextBlock Text="{Binding Name}" />
            </HierarchicalDataTemplate>
            <HierarchicalDataTemplate DataType="{x:Type solution:ItemViewModel}" ItemsSource="{Binding Children}">
                <TextBlock Text="{Binding Name}" />
            </HierarchicalDataTemplate>
        </TreeView.Resources>
    </TreeView>
</UserControl>